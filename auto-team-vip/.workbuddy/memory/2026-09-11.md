# 2026-09-11

## auto-team-vip v2 方案核实（未开工，仅评审）

- 已通读 `docs/auto-team-vip-redesign.md`（v2 快照+租约模型，已定稿）、v1 `index.js`、`worker.js`、`wrangler.toml`、桌面日志。
- git 确认：仓库根 `D:/Code/EchoMusicPlugins`（单仓包含插件+worker+docs），当前在 `vip` 分支，工作区干净，设计文档已提交。
- 核实结论：架构成立，可开工；发现 2 个设计级补充点：
  1. v2 丢失了 v1.1.2 的"join 失败后立即领新码重试"，建议 result=failed 后当轮回 ASSIGN（限 2~3 次）。
  2. full/invalid 失败仅 10min 冷却不纠偏状态，无人快照的死队会反复浪费分配（最长 6h 至 stale）；建议 full 视为酷狗观测直接置 member_count=3/status=full（保守条件：分配时可用名额=1），invalid 长冷却 24h。
- 实现级注意：runFullFlow 必须内部全捕获（否则 runChain 变 rejected 后所有触发点静默失效）；uid 需从随机 storage uid 迁移为酷狗 userid；alarm 清理需覆盖 v2 全部表；teams.status 的 stale 不落库改查询推导。
- 待用户决策上述 2 个补充点后更新设计文档再开工。

## v2 实施完成（同日晚）

- 用户确认采纳：当轮重试（≤3 次）、失败即纠偏（full 最后名额置满员 / invalid 24h 冷却）、先更新文档再开工。
- vip 分支 4 个提交：478a714（文档二次复核补充）→ 43e631f（worker v2 重写 + wrangler.toml）→ f37c40e（插件 v2 重写 + manifest 1.2.0 + README）。
- worker.js：API v2 五端点、DO SQLite 六表、租约状态机、失败即纠偏、令牌桶限速、token 永不重签、alarm 全表清理。node --check 通过。
- index.js：单 Runner 串行链（全捕获）、①→⑧ 流程、uid=userid、心跳、期次切换、401 停用、退避。node --check 通过。
- 实施中发现并修复：网络异常误分类为 invalid 导致好队被 24h 冷却（补 network 分支）；worker _exec 未返回游标；poolLine 非响应式；登录触发缺延迟。
- 尚未部署：需用户 `wrangler deploy`（Worker 先行，v1 客户端将 403 提示更新）；未做真机联调，建议先本地 wrangler dev 验证。

## 数据模型澄清与 /v2/admin/data 端点（夜）

- 用户三问澄清：码池不存成员名单/昵称/队员 userid，仅存 code+队长uid+member_count（快照模型天然粒度）；插件外手动加入的队伍会经加入者快照的 joined 维度入池（外部码入池为既定决策）；失效判定是队伍级（full 终态 / fail_until 冷却 / 6h stale 查询推导），组队码不单独标记。
- 原 v2 无全量明细接口（v1 stats 已删，health 仅聚合）→ 新增 `POST /v2/admin/data`（85a5ecd）：X-Admin-Token 门禁、只读、返回全量队伍/用户(无 token)/租约/事件/summary；文档 §6.8 与 README 已同步。
