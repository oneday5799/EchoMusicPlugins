# 2026-09-12

## 成员名单/昵称/奖励落库（dda2d24）

- 用户要求码池记录成员名单、昵称、每人获得的奖励。日志核实：`/team/my/info` 的 `member_list[]` 自带 `userid/nick_name/role(1队长|2队员)/vip_desc("获得7天VIP"等，即每人奖励)/user_pic`，插件此前只取人数。
- 插件：`normalizeTeamInfo` 新增 `toMembers` 提取（userid 必填、nick/reward 截 48 字符、role 归一 1|2），快照 `created/joined` 均附带 `members`。
- Worker：SCHEMA_VERSION 2→3（版本不符时**全部业务表** DROP 重建，修复了旧逻辑只 drop codes/rate_limit 会残留旧 teams 表缺列的隐患）；teams 加 `members_json`（sanitizeMembers 净化：≤3 人白名单截断；快照未携带时保留旧值）；`/v2/admin/data` 输出各队 members（userid/nick/role/reward）。
- 隐私边界保持：成员明细仅 X-Admin-Token 站长端点可见，`/v2/status` 等普通端点不变；user_pic 不采集。
- 最坏快照载荷实测 1351 bytes < 4096 上限。文档 §4/§5/§6/§7 与 README 同步；两个 JS node --check 通过。
- 注意：v2 尚未部署，schema v3 直接随首次部署生效；若之前本地 wrangler dev 建过 v2 DO，数据会被重建（可接受）。

## 数据查看方式（85b7a71）

- Worker：/v2/health 与 /v2/admin/data 开放 CORS + OPTIONS 预检（仅这两个 Token 门禁端点，无 Cookie 凭证，ACAO=* 安全）。
- 新增 `team-pool-worker/admin.html` 本地看板：API 地址/期次/Token 三项输入（localStorage 记忆），聚合概览卡片 + 队伍表（成员昵称/uid/角色/奖励 chips）+ 租约/用户/事件折叠表 + 30s 自动刷新；X-Plugin-Version 固定高版本号过门禁；全部用户数据 HTML 转义。
- 查看路径给用户：部署 Worker → wrangler secret put ADMIN_TOKEN → 打开 admin.html 或 curl/Invoke-RestMethod 调 /v2/admin/data；404 表示 token 未配置或不符。
- admin.html 内联 script 用 new Function 提取校验语法通过。

## v2 实现全面审查（vip 分支 @7f7839f）

- 范围：docs/auto-team-vip-redesign.md vs index.js(1.2.0) / worker.js / wrangler.toml / admin.html / README。
- 一致性结论：Worker 与 §6 高度一致（租约状态机+时间戳过期+懒清扫+alarm 双保险、快满优先+DTO 选队、full/invalid 纠偏冷却、令牌桶、401 永不重签、v1 端点全下线、双管理端点）；插件与 §7 一致（单 Runner 串行链+10s 合并、全捕获、快照协议、当轮重试≤3、already_joined 纠偏、指数退避、按 uid 存 token）。wrangler.toml 的 `[exports.PeriodPool]` 写法与 v1 线上部署相同，无兼容问题。
- **修复 1 个阻断级 bug**：index.js 渲染函数调用未定义的 `poolLineText()`（原为 setup 期一次性 IIFE `poolLine`，非响应式且引用错名）→ 打开面板 Vue 渲染必抛 ReferenceError。已改为渲染期调用的 `poolLineText` 箭头函数，node --check 通过，未提交。
- 遗留建议（未改，待用户拍板）：① classifyJoinError 未识别错误默认 kind=invalid → 服务端 24h 冷却误伤好队，建议拆 transient/invalid；② _upsertTeam 单调性 event 仅在 prevStatus=full 时记（设计要求任意减少都记）；③ 真实 WAF 403（非 version）静默退避无提示，设计 §6.7 要求提示切 workers.dev；④ join 服务端幂等查在 last_joined_code 检查前，120s 窗口内极端场景会返回旧租约（可自愈）。
- 部署提醒：本地 vip 领先 origin/vip 14 提交未推送；Worker v2 未部署；部署前需 ADMIN_TOKEN secret + WAF 跳过规则（/v2/* 或 X-Plugin-Version），且与插件发版紧凑衔接（MIN_CLIENT_VERSION=1.2.0 会使 v1.1.x 全部 403）。

## 三次复核修订落地（4f1f57f）

- 用户拍板实施审查建议 1+2+4（第 3 项 WAF 403 用户已自行改 WAF 规则解决，不再处理）。
- 插件 index.js：`classifyJoinError` 默认 `transient`，`invalid` 仅认显式证据（不存在/无效/已解散/组队码错误），20006 归 full，joined 关键词次之；`poolJoin` 增可选 `exclude_codes`（≤3）；重试循环维护 `excludedCodes`（kind=network 不避让，重试同队合理）。
- Worker worker.js：`join()` 先查 `last_joined_code` 再查活跃租约（快照观测优先，消除 120s 残留租约窗口）；选队 SQL 增 `exclude_codes` 动态 `NOT IN`（净化 ≤3 项、仅本次生效不落库）；`_upsertTeam` 任意 member_count 减少均记 events（原仅 prev=full 时记）。
- 文档：redesign.md 头部三次修订行 + §6.1 请求示例 + §6.4 transient 语义 + §7.4 重试说明 + §11 三次修订记录；README 匹配策略行补充 transient。SCHEMA_VERSION/MIN_CLIENT_VERSION 均不变（无表结构/协议破坏性变化）。
- 单提交 4f1f57f（合并了上一轮 poolLineText 面板修复），双端 node --check 通过。本地 vip 领先 origin/vip 15 提交，仍未推送；Worker v2 仍未部署。
- 实测校准（桌面 6 份场景日志）：酷狗 join 数值错误码 143001=队伍不存在、143004=满员、143010=已是成员；错误字段为顶层 `error_msg`、`data` 为空串；业务失败走 HTTP 502（body 可读，按 body status 判定）；成功响应 data 也为空串，成队详情须再查 my/info。
- 修复 classifyJoinError 两个阻断缺陷：①字段名读 `msg` 而非实测 `error_msg`，关键词全部失效；②`body?.data ?? body` 因 data="" 不回退顶层，error_code 也读不到——修复前实测三类错误全误判 transient。修复后数值码优先（143004/143010/143001，20006 保留兼容）+ 关键词兜底，already_joined 弃"已经"泛匹配改"是队伍成员"。8 项单测全过，已提交。
- 环境备注：本机 shell 工具链异常（bash 缺 coreutils、PowerShell 输出捕获为空且 Remove-Item 时灵时不灵），删临时文件用 `node -e fs.unlinkSync` 最可靠。

## 日常开发审查（vip @24c277a，方案 vs 实现终检）

- 一致性：worker.js/index.js/wrangler.toml/README/admin.html 与方案 §6/§7 一致，三次复核修订全部落地；join 选队 SQL 参数与占位符逐一核对匹配（7+N）；无 v1 残留引用；版本口径统一 1.2.0。
- 新发现（待用户拍板）：① alarm() 空池 deleteAll 后同内存实例表丢失（构造器不重跑→后续请求 500），建议 deleteAll 后补 this._init()；② 心跳被 periodActive 硬门控，首次 GUARD 失败后不自愈（期次结束也不会自动恢复下一期），建议区分 GUARD 失败/期次未开启；③ createTeam 响应被丢弃，失败无提示；④ 租约分配后未按 §6.3 重设 alarm（正确性由懒清扫保证，仅与文档不符）；⑤ 同账号多设备登录后者整期 401（设计取舍，建议文档注明）。
- 状态：本地 vip 领先 origin/vip 18 提交未推送；Worker v2 仍未部署（上线 checklist：ADMIN_TOKEN secret → 部署 → /v2/health 验证 → 插件 1.2.0 紧凑发版）。

## 加固落地与推送（b5b2680）

- 用户拍板全部实施：①alarm() 空池 deleteAll 后补 `this._init()`（防同内存实例缺表 500）；②join/joinResult 后调用 `_scheduleNextAlarm()`（与 §6.3 对齐）；③插件心跳改 `periodState`（unknown/error/active/inactive）驱动——GUARD 失败按常规间隔自愈重试，期次未开启每 30min 低频探测（`period_probe`，下一期自动开始）；④建队失败 parseJoinResponse 校验并 `setLastError`，不阻断分配。⑤README 增多设备 401 说明；redesign.md §11 增「实现加固」小节。
- 双端 node --check 通过；提交 b5b2680 并推送，`git ls-remote origin vip` 证实远端=b5b2680，19 个提交全部上线。
- **环境坑（重要）**：本机存在 WorkBuddy 工作树（`C:/Users/Oneday/WorkBuddy/Worktrees/auto-team-vip/origin-vip-03a16790`，钉在旧 71e1166），后台进程会持续把 `refs/remotes/origin/vip` 重置回旧值——fetch/update-ref 显示成功但立即回读仍是旧值，`git status` 恒显 ahead。**判断推送是否成功必须用 `git ls-remote`，不要信本地 tracking ref / status 显示**。
- 下一期次待办不变：Worker v2 部署（ADMIN_TOKEN secret → 部署 → admin.html 验证 → 插件 1.2.0 发版紧凑衔接）。

## docs 目录适配核对（用户问询）

- docs/ 共 7 份：6 份 EchoMusic 框架参考（plugin-development/titlebar/tasks/floating-windows/graphics/backups）+ 1 份项目设计（redesign.md，已同步）。
- 插件 API 用法逐项对照框架文档：titlebar.register、net.request+unrestrictedNetwork、kugouVerification、ctx.vue/ui.components/storage/teleport/css.inject/toast/pinia/electron、manifest schema、生命周期清理——**全部符合文档，无需适配改动**。
- 备忘：①文档称跨域/HTTPS 降级重定向会剥离 Authorization/Cookie 类敏感头，可选给 poolRequest 加 `maxRedirects: 0`（实际风险≈0，未改）；②`ctx.kugou`（kugouApi 能力）按 src/renderer/api/*.ts 动态生成命名空间，若宿主未来暴露 team 模块可评估迁移（现用 ctx.electron.api.request 手工鉴权头，实测已校准，不迁移）；③ctx.tasks 任务中心不适合心跳轮询场景，维持 setInterval+deactivate 清理。

## v1.1.2 调试日志版（桌面交付）

- 用户要在真实环境采集酷狗 /team/join 的实际错误码/文案，校准 v2 classifyJoinError 关键词表。
- 从 main 分支（git show 提取，未动工作区）取 v1.1.2 三件套，输出到 `C:\Users\Oneday\Desktop\auto-team-vip-1.1.2-debug\`：`_DEBUG=true` + joinTeam 无条件前后日志（[TEST] join →/←，含原始 body JSON）+ runOncePool 两处分类后 [TEST] classified/retry classified 日志 + manifest description 调试标记（version 保持 1.1.2 过门禁）。功能逻辑零改动，node --check 通过。
- 附测试说明.txt：看 Console 过滤 auto-team-vip，回收 [TEST] join ← 原始 JSON；注意 v2 Worker 部署后此版本码池功能会 403，但酷狗侧日志不受影响。

## 日常开发复核（vip @9162cd1，方案 vs 实现再审查）

- 远端核实：`git ls-remote origin vip`=9162cd1=本地 HEAD，推送链路健康；`git status` 恒显 "ahead 20" 仍为 WorkBuddy worktree 污染 tracking ref（origin/vip 本地 ref 钉在 71e1166），以 ls-remote 为准。
- 全量复读 redesign.md §1-11 / index.js / worker.js / wrangler.toml / README / admin.html：§5-§8 模型、§6.4 选队 SQL（含 exclude_codes 占位符 7+N 逐一核对）、§6.6 永不重签、alarm 清理、令牌桶、版本门禁、admin 端点门禁均与方案一致，无 v1 残留，版本口径统一 1.2.0。
- **修复 b5b2680 两处"声称落地但只落一半"**（提交 9162cd1，双端 node --check 通过，已推送）：
  1. index.js `periodState` 只有初始化+心跳读取、全文件无赋值路径 → 补 GUARD 失败=error、期次未开启=inactive、进行中=active 三处；30min 低频探测（period_probe）自此实际生效（README 已宣传该行为）。
  2. worker.js `join()` 分配租约后未调 `_scheduleNextAlarm()`（仅 joinResult success 有）→ 已补，对齐 §6.3/§11；此前正确性由懒清扫+时间戳兜底，无超发风险。
- redesign.md §11 增「实现加固补全（2026-09-12 二次复核）」记录审计线索。
- 未改动、留观察的建议项：①teamRequest 验证码触发条件 `eventId && (20028 || failed)` 偏宽——业务失败（143001/143004/143010，HTTP 502 body.status=0）若响应带 eventId 也会弹验证码并重试同样注定失败的 join，可考虑对已知业务错误码跳过验证码（需实测确认失败响应是否携带 eventId，暂不动）；②/v2/status 端点插件未使用（快照已覆盖保活+聚合需求），属预留无害，可文档标注；③429 与网络故障同走 5/15/30min poolDown 退避，稍重但可接受。
- 下期次待办不变：Worker v2 部署（ADMIN_TOKEN secret → 部署 → admin.html/health 验证 → 插件 1.2.0 紧凑发版 + WAF /v2/* 跳过规则已由用户自行配置）。

## 日常开发复核（vip @422b70d，第四轮全量审查）

- 远端健康：`git ls-remote origin vip`=422b70d=HEAD（再次证实 status 的 "ahead 22" 为 tracking ref 污染假象）。
- 全量复读 redesign.md / index.js / worker.js / wrangler.toml / README / admin.html：历次修复（poolLineText、classifyJoinError 数值码优先、periodState 三处赋值、alarm 补调度、deleteAll+_init、exclude_codes、单调性审计、join 顺序对调）逐一确认落地；join 选队 SQL 参数序（now/fresh/now/uid/...N/now/uid/now）再次核对无误；admin.html XSS 转义无遗漏（用户数据全走 esc()，数值列为服务端受控）；版本口径 1.2.0 四处统一。
- 本轮新发现（待用户拍板，均为完善项非阻断）：
  1. **period_id 无格式校验**（worker.js fetch 入口）：任意非空字符串直达 getByName → 可批量建空 DO 绕过 per-DO 限速。建议加 `^[A-Za-z0-9_-]{1,32}$`。
  2. `LOGIN_RUN_DELAY_MS` 死常量：登录 watcher 实际立即触发，方案 §7.2 的 "+2s" 未落地（heartbeat 可自愈，影响极小）。
  3. worker join() 的 NOT EXISTS 限定 `l2.code = t.code`（按队），方案 §6.4 SQL 为全局不限 code——实现更合理（防 confirmed 租约+快照清空后永久锁死），建议改文档对齐实现而非改代码。
  4. 限速在鉴权前：第三方可灌已知 uid 令牌桶致 429（小规模影响低；可选 join/result/status 改先 auth 后限速）。
  5. 401 停用粒度按 periodId（poolAuthDisabled），多账号设备上一账号 401 连坐全部账号，与 token 按 uid 分键的多账号意图不一致，可改 `periodId:uid` 键。
- 结论：无需更好方案——快照+租约+DO 每期一实例模型与需求复杂度匹配，备选（D1/KV/签名鉴权）均不更优；**当前最大差距是 Worker v2 尚未部署**，代码已全部就绪。
- 用户拍板全部实施（同日落地）：
  1. worker.js fetch 入口 period_id 校验 `^[A-Za-z0-9_-]{1,32}$`（400 bad_period_id）。
  2. worker.js join/joinResult/status 改先鉴权后限速（snapshot 保持先限速防 uid 洪泛建户，注释已注明两种顺序的理由）。
  3. index.js poolAuthDisabled/disabledPeriods 键改 `periodId:uid`，三处调用点（doSnapshot 判定、doSnapshot 401、join 401）同步传 uid。
  4. index.js 登录 watcher 补 `setTimeout(..., LOGIN_RUN_DELAY_MS)`（死常量复活，对齐 §7.2）。
  5. redesign.md：§6.4 SQL 改按队限定（`l2.code = t.code`、`t.captain_uid <> :uid`）+ 补"已加入判定以快照为准"bullet；§6.5 补限速/鉴权顺序与 period_id 校验两条；§7.4 补「期次:账号」粒度；§11 增「第四次复核修订」记录。
- **环境坑再现**：本会话 Edit 工具多次报 success 但内容被回吞（EBUSY 文件锁竞争，共 4 处：isPoolDisabled/doSnapshot 调用点、joinResult 限速顺序、§6.5/§7.4 文档）——**多文件批量编辑后必须用 `git diff` 全量核对，不能只信工具成功返回**。已全部补齐，双端 node --check 通过。

## 日常开发审查（vip @d24e812，第五轮全量审查）

- 远端健康：`git ls-remote origin vip` = d24e812 = HEAD（`git status` 的 "ahead 24" 仍为 WorkBuddy worktree tracking ref 污染假象）；双端 node --check 通过。
- 全量复读 redesign.md / index.js / worker.js / wrangler.toml / README / admin.html：四轮复核修订全部落地逐项验证；join 选队 SQL 参数序（now/fresh/now/uid/...N/now/uid/now）再次核对无误；admin.html XSS 转义无遗漏。
- 本轮新发现（待用户拍板，均为完善项非阻断）：
  1. `_scheduleNextAlarm` 覆盖式 `setAlarm(next)` 会把已有更早的租约到期 alarm 推迟（先分配 A 到期 T0、再分配 B 到期 T1>T0 时，alarm 从 T0 被覆盖为 T1）——无名额泄漏（选队/幂等均按 expires_at 时间戳 + 懒清扫兜底），仅 alarm 回收与 expire 审计不及时；建议 setAlarm 前与 `getAlarm()` 取 min。
  2. `snapshot()` 未调用 `_sweepExpired()`（join/result/status/health/adminData 均有）——无正确性影响（迟到确认 UPDATE 的 status IN 本就覆盖 pending/success/expired），纯一致性小点。
  3. 429 与网络故障同走 5/15/30min poolDown 退避（前轮遗留项 ③ 具体化）：令牌桶持续 1rps，60~120s 短退避即可恢复，当前过重。
  4. `/v2/status` 端点插件未调用、无消费者（快照已覆盖保活+聚合需求，预留无害）——建议方案 §6.1 与 README 标注预留。
  5. README「码池部署」章节缺上线 checklist（wrangler deploy → ADMIN_TOKEN secret → /v2/health 验证 → 插件 1.2.0 紧凑发版 + WAF /v2/* 跳过规则确认）。
- 结论：无需更好方案（维持第四轮结论：快照+租约+DO 每期一实例模型与需求复杂度匹配）；**最大差距仍是 Worker v2 未部署**，期次 288 至 09-15 结束。
- 用户拍板全部实施（同日落地，提交 3dd2311 + 65a61ee，已推送，ls-remote 证实远端=65a61ee）：①worker `_scheduleNextAlarm` setAlarm 前与 `getAlarm()` 取 min；②worker `snapshot()` 入口补 `_sweepExpired()`；③index.js 新增 `POOL_RATE_LIMIT_BACKOFF_MS=60s`/`pool429Until`/`poolRateLimited()`，`poolAvailable()` 同时校验两退避，doSnapshot 与 join 失败分支各加 429 短退避（poolUp 重置）；④redesign §6.1 status 预留标注 + §7.4 退避句 + §11 第五轮记录；⑤README 部署章节改 6 步上线 checklist。
- **Edit 回吞坑再次出现**：doSnapshot 的 429 分支首次 Edit 报 success 但 git diff 中缺失——**git diff 核对再次救场**，补齐后双端 node --check 通过。

## 日常开发复核（vip @2256a7e，第六轮全量审查）

- 远端健康：`git ls-remote origin vip` = 2256a7e = HEAD（status 的 "ahead 27" 仍为 worktree tracking ref 污染假象）；双端 node --check 通过；admin.html 的 esc() 转义与 X-Admin-Token/X-Plugin-Version 双头抽查通过。
- 全量复读 redesign.md / index.js / worker.js / wrangler.toml / README：五轮复核修订全部在代码中逐项确认落地；join 选队 SQL 参数序（now/fresh/now/uid/...N/now/uid/now）核对无误；版本口径 1.2.0 四处统一。
- 本轮新发现（待拍板，均非阻断）：
  1. teamRequest 验证码条件 `eventId && (20028 || failed)` 可加业务错误码豁免（143001/143004/143010/20006 不弹验证码）——白名单式豁免，20028 正途与未知新码行为不变，较上轮"暂不动"更安全可落地；
  2. snapshot 按 uid 限速不防 uid 轮换洪泛建户（设计 §6.6 已接受的风险，可选 Worker 层 per-IP/全局建户上限，规模小建议不做）；
  3. ADMIN_TOKEN 比较非常量时间（可选 crypto.subtle.timingSafeEqual，实际风险≈0）；
  4. doSnapshot 的 needUpdate 路径未设 uiState.poolDisabled（纯外观，版本错误提示已另有展示）；
  5. health() events_24h 未计 'correct'/'assign' kind（观测完整性小点）；
  6. 上线后调优项：stale 阈值 6h→1~2h（§5.3 预留）。
- 结论：无需更好方案——第四、五轮"快照+租约+DO 每期一实例"结论独立复核后维持；**最大差距仍是 Worker v2 未部署**，期次 288 至 09-15 结束，按 README checklist 在下期开始前上线（部署后 v1.1.x 含桌面调试版码池功能立即 403）。
- 用户拍板"只做验证码豁免"（其余小项不改）：落地提交 **7e95ba2**（index.js 新增 `BIZ_JOIN_CODES` 白名单，teamRequest 验证码条件改 `20028 || (failed && !BIZ_JOIN_CODES.has(errorCode))`，git diff 逐行核对 + node --check 通过，已推送，ls-remote 证实远端=7e95ba2）。第 2~6 项按拍板保持现状。

## 日常开发复核（vip @7e95ba2，第七轮全量审查）

- 远端健康：`git ls-remote origin vip` = 7e95ba2 = HEAD（status 的 "ahead 28" 仍为 worktree tracking ref 污染假象）；双端 node --check 通过；未提交变更仅 memory 日志 1 行。
- 全量复读 redesign.md / index.js / worker.js / wrangler.toml / README / manifest：六轮修订全部落地逐项确认；join 选队 SQL 参数序（now/fresh/now/uid/...N/now/uid/now）第七次核对无误；admin.html innerHTML 全走 esc() 再确认；manifest 1.2.0 / MIN_CLIENT_VERSION 1.2.0 / kugouVerification 能力声明齐全。
- 本轮新发现（待拍板，仅 1 项小完善）：
  1. **runFullFlow 重试循环缺响应守卫**（index.js `⑤→⑦` 循环）：`poolJoin` 返回 `ok:true` 但既无 `reason` 也无 `code`/`lease_id` 时（协议异常/中间层篡改场景，当前服务端三个正常分支均有 code），`String(d.code)` = "undefined" 会被当组队码发给酷狗 join。建议加一行守卫：`if (!d.lease_id || !d.code) { setLastError("码池响应异常，稍后自动重试", "pool_bad_response", {...}); return; }`。纯防御性，无行为变化。
- 用户拍板"加一行守卫直接 return"，同日落地（提交 **343cad0**，git diff 逐行核对 + node --check 通过）。落地时修正发现描述的一处误差：`!d.code` 原本已被上游 `pool_empty || !d.code` 分支拦截（"undefined" 发给酷狗的场景实际不会发生），守卫的真实价值是拦 `!d.lease_id`——code 有效但缺 lease_id 的协议异常响应，避免无租约的 join 与空 lease_id 的 result 回报（后者会被服务端 400）。双条件显式检查，语义完整。
- 方案评估：独立复核后维持前几轮结论——快照+租约+DO 每期一实例模型与问题本质（酷狗真相源+入队不可逆+供需 2:1）匹配，备选（D1 全局库无单线程原子性/KV 最终一致/CRON 批匹配延迟高/签名鉴权密钥管理复杂）均不更优；每期百~千级规模远低于需要分片的量级。
- **最大差距仍是 Worker v2 未部署**：期次 288 至 09-15（周二）结束，按 README 六步 checklist 在下期开始前上线（ADMIN_TOKEN secret → deploy → /v2/health 验证 → WAF 确认 → 插件 1.2.0 紧凑发版 → admin.html 核对）。部署后 v1.1.x 含桌面调试版码池功能立即 403。

## 日常开发复核（vip @7e0a278，第八轮全量审查）

- 远端健康：`git ls-remote origin vip` = 7e0a278 = HEAD（status 的 "ahead 30" 仍为 worktree tracking ref 污染假象）；双端 node --check 通过。
- 全量复读 redesign.md / index.js / worker.js / wrangler.toml / README / admin.html / manifest：七轮修订全部落地逐项确认（poolLineText、BIZ_JOIN_CODES 豁免、classifyJoinError 数值码优先、exclude_codes、periodState 三处赋值+心跳驱动、429 短退避、lease_id/code 双守卫、401 期次:账号粒度、SCHEMA_VERSION 3 全表重建、deleteAll+_init、alarm 取 min、snapshot 懒清扫、join 顺序对调、单调性审计、先鉴权后限速、period_id 校验、六步 checklist）；join 选队 SQL 参数序（now/fresh/now/uid/...N/now/uid/now）第八次核对无误；admin.html 用户数据插值点 esc() 全覆盖（member_count/inflight 为服务端受控数值）；manifest 1.2.0 / MIN_CLIENT_VERSION 1.2.0 / kugouVerification 齐全。
- 本轮新发现（1 项，已直接落地）：`joinResult` 的 `lease_not_found` 是唯一不带 status 字段的 4xx 分支（missing_uid=400 / unauthorized=401 / unknown_result=400 均有）→ 以 HTTP 200 返回 ok:false。客户端不消费 result 响应、零实际影响，纯协议一致性。提交 **bb3afa8**（一行：`{ok:false, status:404, ...}`），git diff 核对 + node --check 通过，已推送，ls-remote 证实远端=bb3afa8。
- 方案评估：独立复核后维持前七轮结论——快照+租约+DO 每期一实例模型与问题本质匹配，无更优备选；代码侧无遗留开放问题，历次"留观察项"（stale 阈值 6h→1~2h 调优、snapshot 按 uid 限速不防 uid 轮换、ADMIN_TOKEN 非常量时间比较）维持上线后观察的拍板。
- **最大差距仍是 Worker v2 未部署**（代码全部就绪）：期次 288 至 09-15（周二）结束，按 README 六步 checklist 上线。

## vip 合并 main + 推送 1.2.0（9716cbc → main 88e11aa）+ 本地 .git 损坏修复

- **合并结果**：main = 88e11aa（vip 29 个提交 fast-forward 后再 merge 远端 main 9bdc4fe——远端含 openrgb 插件/docs/tcp.md 等用户经 GitHub 侧合入的 3 个提交，ort 自动合并无冲突），manifest=1.2.0 已随 main 上线，`git ls-remote` 证实远端 main=88e11aa、vip=9716cbc。
- **重大环境事故——本地 .git 损坏**（与既有 EBUSY/Edit 回吞、tracking ref 污染同源，本机磁盘对 git 进程写入的文件存在丢失/回滚现象，node/Read 工具写入的文件正常）：
  1. 症状链：merge 时报 `9d478936 is not a valid object / fatal: stash failed` → 随后 git 全部报 `not a git repository`。
  2. 实际损坏：`.git/refs` 整个目录消失；一个 pack（271cd476）只剩 idx 无 pack；multi-pack-index 失效；剩余 pack 读不出 42c08fd 等老对象；工作区 docs/ 下 6 个框架文档消失（backups/floating-windows/graphics/plugin-development/tasks/titlebar）。packed-refs 残留旧值且含 `.bak`、`opencode` 等非常规条目。
  3. 修复路径（全部成功）：node fs 确认 refs 目录 ENOENT → 读 `logs/refs/heads/{main,vip}` reflog 找回正确 ref 值（均 9716cbc）→ 重建 refs 指向旧值仍报 bad object HEAD（对象确实丢失）→ **隔离全部 pack + 本地分支 ref + packed-refs** 到 `.git/orphaned-pack-backup/`、`.git/refs-backup-20260912/` → `git fetch origin` 全量重建对象库 → 重写 refs/heads 与 refs/remotes/origin 四个 ref → `git restore docs/` 恢复 6 个文档 → fsck 仅剩 reflog 历史条目警告（无害）。
  4. **关键经验**：git 写入的 refs 会消失而 node 写入的保留时，直接用 node 手写 ref 文件（SHA+`\n`）；fetch 协商被死对象（packed-refs 的 origin/dev）阻塞时隔离 packed-refs；`not a git repository` 但 `.git/HEAD+config` 可读 = refs 层损坏而非目录丢失；一切以 `git ls-remote` 为准；本机 shell 用 `git -C "D:/..."`（`/d/` 映射间歇失效）。
  5. 遗留：orphaned-pack-backup 与 refs-backup-20260912 留作取证可后续删；WorkBuddy worktree 污染源仍在（origin/vip tracking ref 可能再次被重置，判断以 ls-remote 为准）；**建议用户检查 D 盘健康（chkdsk/SMART）与杀软/同步盘对 D:\Code 的实时干预**——git 写入文件消失是磁盘/软件级异常信号。
- 后续日志提交继续在 vip 分支做（惯例不变），main 只接收合并。

## 并发损坏事故补充 + 本地仓库修复完成（03:45-03:55）

**事故补充（上轮结论）**：另一进程并发操作本仓库证据确凿——FETCH_HEAD 03:27:01、新 pack c9b0e179 03:27:02、ORIG_HEAD 03:32:33 均非本会话写入，`.git/opencode` 残留文件为线索，orphaned-pack-backup/ 取证目录事后也被其清理。当时主线成果已锁定远端：main=6f4cc01（GitHub API 服务端合并，parents 88e11aa+28a0bae）、vip=28a0bae，**1.2.0 已随 main 上线**。

**修复过程（本轮重试，成功）**：
1. refs 目录再次被清空 → node 手写 heads（main/vip/workbuddy）+ remotes（origin×4）恢复仓库识别；
2. 对象库缺 88e11aa/28a0bae/6f4cc01（9716cbc/42c08fd/9bdc4fe 在 c9b0e179 pack 中幸存）→ 删 tracking refs 强制全量 fetch；
3. TLS 连环故障：schannel 报 CRYPT_E_NO_REVOCATION_CHECK（全局已配 schannelcheckrevoke=false 仍复现）、openssl 后端报 unable to get local issuer certificate；**显式 `-c http.sslBackend=schannel -c http.schannelCheckRevoke=false` 后 fetch 成功**，两配置已写入本仓库 .git/config 防复发；
4. `reset --hard 6f4cc01` 完成：工作区干净，docs×8 + tests×5 全部恢复，HEAD=origin/main，tracking refs 正常；
5. 本节日志随 vip 提交推送并合并到 main，补完上轮被事故打断的"日志同步到 main"。

**遗留**：①找出后台操作 D:\Code\EchoMusicPlugins 的程序；②D 盘健康检查（chkdsk/SMART）与杀软/同步盘排查；③Worker v2 部署（期次 288 至 09-15 结束，下期开始前按 README 六步 checklist 上线）。

## 破坏进程复发（04:12）与远程化收尾

- 04:12:25-26 另一进程再次动手：ORIG_HEAD 与 index 被写、refs 第三次被清空、新增孤儿 pack-b2948056.idx（只有 idx 没有 pack，被中断的 repack 残骸）。本地 merge vip→main 撞车失败（a808ecc not a valid object / stash failed）。03:55 那轮的稳定结论作废——该进程是间歇性发作，不是已停止。
- 应对：本地零 git 写入，全走 GitHub API——vip=e14abc7 已推送；main 经服务端合并为 f1a2986；本节日志经 contents API 提交到 vip 再服务端合并进 main。
- 环境不稳期间固定手法：gh api 完成一切提交/合并，node 手写 refs 恢复本地可读，禁用本地 commit/merge/checkout 等写操作。
